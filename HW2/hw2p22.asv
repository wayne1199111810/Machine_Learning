clear, clc;
load q1x.dat;
load q1y.dat;
nIters = 500;
[m,n] = size(q1x);
d = 1e-8;
rate = 0.001;
w = zeros(3, 1);
x = [ones(m, 1), q1x];


for i=1:nIters
    h = ones(m,1)./(ones(m,1)+exp(-x*w));
    w = w - rate*x
end